// Package render connects Markdown parsing to PDF drawing.
//
// It owns page layout, theme selection, light and dark palettes, line wrapping,
// pagination, and block-specific rendering for headings, paragraphs, lists,
// code blocks, blockquotes, rules, and tables.
package render
